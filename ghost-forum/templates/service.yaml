apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghost-forum.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "ghost-forum.name" . }}
    helm.sh/chart: {{ include "ghost-forum.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ghost-forum.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
